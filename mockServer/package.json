{
	"scripts": {
	    "mock": "json-server --watch mock/db.js --routes mock/routes.json --m mock/post-to-get.js --port 9999"
	  },
	"dependencies": {
		"faker": "^3.1.0",
		"json-server": "^0.9.4"
	}
}